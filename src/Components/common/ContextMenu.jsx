import React from "react";

const ContextMenu = ({ x, y }) => {
  return (
    <div
      // className={`absolute z-50 bg-slate-800 p-3`}
      // style={{ top: `${y}px`, left: `${x}px` }}
    >
      {/* nig */}
    </div>
  );
};

export default ContextMenu;
